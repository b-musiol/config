To change the default branch name for a new git repository, execute

`git config --global init.defaultBranch <new_name>`

For example

`git config --global init.defaultBranch main`

This changes it from the original `master`, which can feel problematic.
