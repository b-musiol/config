To set up credentials there are two types of scope. Global and per workspace.

To set up global credentials, enter:

`git config --global user.name "<name>"`
`git config --global user.email "<email>"`

Or change `name` and `email` accordingly in the global config file, available with:

`git config --global --edit`

Make sure the editor is set to something available when using this.