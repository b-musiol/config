Specifically under linux, the middle mouse button is often bound to pasting the selection.

To disable that, each application must be configured.

In VSCode, to disable it, the user settings must be appended with

```
"editor.selectionClipboard": false
```
